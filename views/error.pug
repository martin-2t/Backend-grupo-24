extends layout

block content
  .aviso
    h2 500 · Error interno del servidor
    p Algo falló al procesar la operación. No se guardó ningún cambio.
    p.nota El detalle quedó registrado en la consola del servidor.
    .barra
      a.btn(href="/panel") Volver al panel
