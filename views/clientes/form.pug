extends ../layout

//- Alta y edición en el mismo form
block content
  - var c = cliente || {}
  - var esEdicion = Boolean(c.id)

  h2= esEdicion ? 'Editar cliente' : 'Nuevo cliente'

  if error
    .error= error

  form.simple(method="POST" action=accion)
    label(for="nombre") Nombre
    input#nombre(type="text" name="nombre" value=c.nombre required)

    label(for="apellido") Apellido
    input#apellido(type="text" name="apellido" value=c.apellido required)

    label(for="email") Email
    input#email(type="email" name="email" value=c.email required)

    label(for="telefono") Teléfono
    input#telefono(type="text" name="telefono" value=c.telefono)

    .barra
      button.btn(type="submit" class=(esEdicion ? 'editar' : ''))= esEdicion ? 'Guardar cambios' : 'Crear cliente'
      a.btn.secundario(href='/panel/clientes') Volver
