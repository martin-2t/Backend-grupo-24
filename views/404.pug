extends layout

block content
  .aviso
    h2 404 · Página no encontrada
    p No encontramos nada en #[code= url]
    p.nota Puede que el recurso haya sido eliminado o que la dirección esté mal escrita.
    .barra
      a.btn(href="/panel") Volver al panel
